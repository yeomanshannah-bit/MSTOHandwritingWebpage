/*
  Beta flags.

  The screener is being trialled by a small group of teachers before the
  Two Term program is finished. Programs are therefore built, saved and
  scoreable in the code — but sealed off in the interface, so testers see
  that the program is coming without walking into a half-finished thing.

  Flip this to true to open programs back up. Everything behind it is still
  wired: nothing was deleted to hide it.
*/
export const PROGRAMS_ENABLED = false;
